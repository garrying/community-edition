{
  "name": "app",
  "version": "0.0.1",
  "description": "",
  "scripts": {
    "watch": "parcel serve index.html --dist-dir ../app",
    "prebuild": "rm -rf ../app/*",
    "build": "parcel build index.html --dist-dir ../app --public-url .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.3.1",
    "parcel": "^2.3.1"
  },
  "dependencies": {
    "@cds/city": "^1.1.0",
    "@cds/core": "^6.0.0-next.4",
    "@cds/react": "^5.6.3",
    "react": "^17.0.2",
    "react-router": "^6.2.1",
    "react-router-dom": "^6.2.1"
  }
}
